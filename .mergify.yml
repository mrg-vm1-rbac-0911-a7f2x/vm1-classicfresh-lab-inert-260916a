pull_request_rules:
  - name: VM1 classic branch-protection status-finalizer freshness
    conditions:
      - title = VM1_CLASSIC_PROTECTION_STATUS_WAKE
      - check-success = VM1_CLASSIC_WAKE
    actions:
      merge:
        method: merge
