pull_request_rules:
  - name: VM1 classic branch-protection midflight freshness
    conditions:
      - title = VM1_CLASSIC_PROTECTION_MIDFLIGHT_LABEL
      - label = vm1-classic-wake
    actions:
      merge:
        method: merge
